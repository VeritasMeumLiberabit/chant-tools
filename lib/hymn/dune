(menhir
 (modules text_parser)
 (flags --table --inspection))

(rule
 (targets text_parser_error_messages.ml)
 (deps text_parser_error.messages text_parser.mly)
 (action
  (with-stdout-to
   %{targets}
   (run menhir --compile-errors %{deps}))))
